# rt584-specific OT.
# MCUboot Single-app mode is set in sysbuild.conf for rt584 (no Direct XIP
# variant of slot1 is built); thread example currently uses Direct XIP for
# rt583 OTA — see CLAUDE.md / RT584 bring-up memory for the layout split.
CONFIG_OPENTHREAD_RT584=y
