description: Rafael RT583 integrated BLE controller (HCI over shared memory)

compatible: "rafael,rt583-bt-hci"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "RT583 BLE"
  bt-hci-bus:
    default: "virtual"
  bt-hci-quirks:
    default: ["no-reset"]
