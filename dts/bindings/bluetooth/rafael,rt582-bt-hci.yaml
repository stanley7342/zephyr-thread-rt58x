description: Rafael RT582 integrated BLE controller (HCI over shared memory)

compatible: "rafael,rt582-bt-hci"

include: bt-hci.yaml

properties:
  bt-hci-name:
    default: "RT582 BLE"
  bt-hci-bus:
    default: "virtual"
  bt-hci-quirks:
    default: ["no-reset"]
